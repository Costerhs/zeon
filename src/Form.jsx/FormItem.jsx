// import React from 'react';

// function FormItem({ reg, holder, hed, type }) {
//   return <div className="item"></div>;
// }

// export default FormItem;
/* <p className="p" style={errors?.reg && style}>
        {hed}
      </p>{' '}
      <input
        style={errors?.reg && border}
        className="inp"
        type={type}
        placeholder={holder}
        {...register(reg, {
          required: 'Поле обязательна к заполнению',
        })}
      /> */
